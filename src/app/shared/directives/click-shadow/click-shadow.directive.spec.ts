import { ClickShadowDirective } from './click-shadow.directive';

describe('ClickShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickShadowDirective();
    expect(directive).toBeTruthy();
  });
});
